
1. refactor UnitView to use BallView instead of GO.
2. make SlotView to register its state.
3. make BallView not switch parents - instead it can just follow the position 

well done - next thing we didnt do yet is visualize the new features in the game
 - i still dont have the final design - but i want a solid plan
  - at the moment we are doing everything with colors for the units
   - later we will take sprites instead - but this has no effect
    - need to plan - is the lock key going to be part of the unit
     (i think so) and we need to have a way to visually draw a link between 2 units
      (can start with line renderer) - and we need something for the pipe -
       and also a PipeView script so that it can update the counter on the pipe - 




       results are very impressive, things to add:
       1. i want the file name to include the following : Lvl_{index}_WinRate{}_Cols_{number of colors}
       2. i dont see any pipes
       3. i dont see more than 1 link
       4. i dont see any lock and key



a couple of things to add - 
1. there is no point in ever putting a blocker in the bottom most row  y=max y -- it blocks nothing. 
2. in the level name also add the features that were used: B_{num of blockers}_P_{num of pipes}_L_{num of links} and so on
and also - in the start where we show the win rate - show it as actual_target.
3. 


pipe cell should not be clickable\playable.

make magnet - this let's you select any unit that is still closed - and it is resolving the first containers 
possible to resolve.

shuffle - takes the second row and switches with the items of the first row.

